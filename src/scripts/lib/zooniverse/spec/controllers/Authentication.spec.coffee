describe 'Authentication', ->
  describe '.setSrc', ->
    it 'changes the iframe\'s location', ->

    it 'updates the external'

  describe '.post', ->
    it 'posts a message to the iframe', ->

  describe '.logIn', ->
    it 'triggers a response from the external', ->

  describe '.logOut', ->
    it 'triggers a response from the external', ->
